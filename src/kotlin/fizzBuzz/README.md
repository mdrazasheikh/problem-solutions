# FizzBuzz

Returns the numbers from 1 through n, replacing multiples of 3, 5, or both with Fizz, Buzz, or FizzBuzz.

Approach: classify each number with divisibility checks.

Complexity: O(n) time and O(n) space for the returned list.
