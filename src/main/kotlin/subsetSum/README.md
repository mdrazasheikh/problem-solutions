# Subset Sum

Finds the first contiguous range whose values add up to a target.

Approach: sliding window over the input values.

Complexity: O(n) time and O(k) space for the returned index list, assuming the input values are non-negative.
