# Merge Two Sorted Lists

Merges two sorted singly linked lists into one sorted list.

Approach: repeatedly link the smaller current node and append the remaining tail.

Complexity: O(m + n) time and O(1) auxiliary space.
