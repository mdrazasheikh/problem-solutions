---
title: Discount Code
slug: discount-code
tags: [string, palindrome, two-pointer]
aliases: [palindrome after insertion, valid discount string]
time: O(n)
space: O(n)
---

# Discount Code

Validates discount strings using palindrome and two-sided insertion rules.

Approach: test the complete value and the two possible partitions with palindrome checks.

Complexity: O(n) time per code and O(n) temporary space for list results.
