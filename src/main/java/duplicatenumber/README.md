# Duplicate Number

Checks whether an integer array contains a repeated value.

Approach: add values to a hash set and stop when an insertion is duplicated.

Complexity: O(n) expected time and O(n) space.
