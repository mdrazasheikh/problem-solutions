# Reverse Number

Prints the digits of an integer in reverse order while handling trailing zeroes.

Approach: repeatedly extract the last digit and print after the first non-zero digit.

Complexity: O(d) time and O(1) space for d digits.
