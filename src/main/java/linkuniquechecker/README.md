# Link Unique Checker

Checks whether two encoded links are equivalent under the normalization rules in the implementation.

Approach: compare normalized suffix and character forms.

Complexity: O(n) time and O(1) auxiliary space.
