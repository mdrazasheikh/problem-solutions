# Angle

Finds the minimum travel cost for the circular arrangement represented by the input.

Approach: evaluate both directions around the duplicated circular sequence.

Complexity: O(n) time and O(n) space.
