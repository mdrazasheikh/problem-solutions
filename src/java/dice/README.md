# Dice

Calculates missing dice rolls needed to reach a requested mean.

Approach: derive the required total and distribute it across the missing rolls.

Complexity: O(n) time and O(n) space for the returned rolls.
