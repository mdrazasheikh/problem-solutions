# Longest Consecutive Sequence

Finds the longest run of consecutive integers in an unsorted array.

Approach: use a set and start counting only at sequence beginnings.

Complexity: O(n) expected time and O(n) space.
