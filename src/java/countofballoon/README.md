# Count Of Balloon

Finds how many times the word `balloon` can be formed from the characters of a string.

Approach: count letters and divide the duplicated letters by two.

Complexity: O(n) time and O(1) space.
